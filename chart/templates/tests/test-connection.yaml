apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "masa-oracle.fullname" . }}-test-connection"
  labels:
    {{- include "masa-oracle.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "masa-oracle.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

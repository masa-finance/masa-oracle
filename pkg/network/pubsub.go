package network

import (
	"context"

	"github.com/libp2p/go-libp2p/core/host"
)

func PubSub(ctx context.Context, host host.Host) error {
	//gossipSub, err := pubsub.NewGossipSub(ctx, host)
	//if err != nil {
	//	return err
	//}
	return nil
}

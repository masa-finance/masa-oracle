package config

var (
	ApplicationVersion string

	// XXX: Bump this value only when there are protocol changes that makes the oracle
	// incompatible between version!
	ProtocolVersion = `v0.5.1.2`
)
